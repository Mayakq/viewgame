version: '3.9'
services:
  postgres:
    image: postgres:16
    container_name: viewgame
    ports:
      - '6500:5432'
    volumes:
      - progresDB:/var/lib/postgresql/data
    env_file:
      - ./.env
volumes:
  progresDB:
